// #include "../minishell.h"

// int dollar(t_env **e, t_command cmd, int line, char **env)
// {
//     char *str;

//     cmd.command++;
//     str = get_env_value(*e, cmd.command);
//     cmd.command = ft_strdup(str);
//     if(str == NULL)
//         return 0;
//     else
//         return commands(e, cmd, line, env);
// }
